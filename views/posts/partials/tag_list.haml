-@tags = Post.by_tag_count(:reduce => true, :group => true)['rows'].sort_by{|row| row['value']}.reverse[0..limit]
%h4 Tags
%ul
  -@tags.each do |row|
    %li
      .count=row['value']
      =link_to "##{row['key']}", "/tag/#{row['key']}"