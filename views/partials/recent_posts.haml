-@posts = Post.by_date(:group => true, :limit => limit, :descending => true)
%h4 Recent Posts
%ul.recent
  -@posts.each do |post|
    %li
      =link_to post.title, url(:post, *post.path)
      %span.time=post.published_at.strftime('%I:%m%P on %D')